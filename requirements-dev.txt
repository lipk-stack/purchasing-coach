-r requirements.txt
pytest>=8
ruff>=0.5          # linting (also wired into CI); config in pyproject.toml
python-docx>=1.1   # used by scripts/make_samples.py to rebuild the sample docx
anthropic>=0.50
