# Runtime — pure Python only, so the app stays portable (no install rights
# needed; see scripts/build_portable.py for the single-file .pyz build).
openpyxl>=3.1

# Optional backends/formats:
# anthropic>=0.50   # only if using the Claude API backend (--backend claude)
# pypdf>=4.0        # only if loading .pdf guidelines
